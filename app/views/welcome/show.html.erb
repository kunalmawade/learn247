<% if current_user.student? %>
	<% render 'student_home', local:{ courses: @courses} %>
<% elsif current_user.instructor? %>
	<% render 'instructor_home', local:{ courses: @courses} %>
<% elsif current_user.admin? %>
	<% render 'admin_home', local:{ courses: @courses} %>
<% end %>